eureka:
  client:
    service-url:
      # MUDANÇA CRUCIAL AQUI 👇
      # Trocamos 'localhost' pelo nome do container 'eureka-server'
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true # Isso aqui é o "auto-map": ele cria rotas automáticas baseadas no nome do serviço no Eureka
      routes:
        - id: player-service-rota
          uri: lb://PLAYER-DOCKER  # Deve ser o nome exato do spring.application.name
          predicates:
            - Path=/players/** # Tudo que chegar em :8080/players/ vai para o seu novo serviço