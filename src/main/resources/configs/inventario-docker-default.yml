eureka:
  client:
    service-url:
      # MUDANÇA CRUCIAL AQUI 👇
      # Trocamos 'localhost' pelo nome do container 'eureka-server'
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true